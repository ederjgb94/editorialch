<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si la petici��n ya viene con /public/, no hacemos nada
    RewriteCond %{REQUEST_URI} !^/public/

    # Mover todo lo que llegue a la carpeta /public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>